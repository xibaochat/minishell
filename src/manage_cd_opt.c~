#include "minishell.h"

void cd_to_home(t_mini *sh)
{
	char    *path;

	path = get_extracted_path(sh->env, "HOME=");
	change_dir(path);
}

void cd_to_current_dir(void)
{
	char *path;

	path = getcwd(NULL, 0);
	change_dir(path);
	free(path);
}
